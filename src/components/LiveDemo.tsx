'use client'

// Removed: live demo section is not part of the redesigned site. The home
// page no longer imports this component; kept as a no-op so old imports
// (if any) don't break the build during the step-by-step migration.
export default function LiveDemo() {
  return null
}
